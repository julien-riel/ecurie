{
  "id": "text-to-speech",
  "title": "Synthèse vocale",
  "description": "Lit un texte avec une voix que le modèle embarque. À distinguer de la synthèse à voix de référence : ici on choisit dans un catalogue fermé, là on fournit un échantillon à imiter.",
  "input": {
    "type": "object",
    "required": [
      "text"
    ],
    "additionalProperties": false,
    "properties": {
      "text": {
        "type": "string",
        "minLength": 1,
        "x-ui": "textarea",
        "description": "Texte à synthétiser."
      },
      "voice": {
        "type": "string",
        "x-ui": "select",
        "x-options-from": "runtime.voices",
        "description": "Voix, parmi celles annoncées par le worker au chargement."
      },
      "speed": {
        "type": "number",
        "minimum": 0.5,
        "maximum": 2.0,
        "default": 1.0
      },
      "seed": {
        "type": "integer",
        "minimum": 0,
        "description": "Graine aléatoire. Les moteurs récents échantillonnent : sans graine, deux synthèses du même texte diffèrent."
      }
    }
  },
  "output": {
    "type": "object",
    "required": [
      "audio"
    ],
    "properties": {
      "audio": {
        "type": "string",
        "contentMediaType": "audio/wav",
        "description": "Chemin du fichier produit, relatif au dossier du job."
      }
    }
  }
}
